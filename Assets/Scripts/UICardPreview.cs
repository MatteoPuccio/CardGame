using UnityEngine;

namespace Assets.Scripts
{
    /// <summary>
    /// Marker component used to distinguish the large "card preview" UI instance from normal UI card views.
    /// </summary>
    public sealed class UICardPreview : MonoBehaviour
    {
    }
}
